# image not found? login using "docker login --username=abc --password=abc https://registry.domain.de"
services: 
  pheonix-customerservice: 
    container_name: pheonix-customerservice
    image: "praveenit219/pheonix-customerservice:latest"
    network_mode: host
    ports: 
      - "10858:10858"
version: "3"